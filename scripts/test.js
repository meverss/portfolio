import data from "../languages/languages.json" assert { type: "json" };

var a = data.languages;

console.log(f_message.blur);

a.forEach((element) => {
  for (let i = 0; i < element.sp.length; i++) {
    let n = element.sp[i];
    console.log(n.name);
  }
});

// document.body.innerHTML += `<div class="box prueba">
// <p>Mi nimbre es: ${n}`
